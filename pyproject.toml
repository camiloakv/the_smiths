[project]
name = "the-smiths"
version = "0.1.0"
description = "Agentic system with LangChain and MCP"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "chromadb>=1.5.5",
    "mcp[cli]>=1.26.0",
    "yfinance>=1.2.0",
]
